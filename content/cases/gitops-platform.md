# GitOps 发布控制台

场景：DevOps / Kubernetes / 发布审计

## 背景

多环境发布依赖人工操作时，配置差异、审核记录、回滚路径和线上状态都很难追踪。发布慢只是表象，真正的问题是责任和状态不可见。

## 交付思路

- 用 Git 作为配置事实源。
- 用 Kustomize 管理多环境差异。
- 用 GitLab CI 做基础校验和策略检查。
- 用 ArgoCD 负责同步、差异展示和回滚。

## 验收口径

- 发布配置是否可审计。
- 线上状态是否能和 Git 对齐。
- 失败后是否能按文档回滚。
- 开发和运维是否能围绕同一套流程协作。
